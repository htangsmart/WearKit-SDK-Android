<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!--用于网络访问-->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!--用于监听短信电话时候根据号码查询联系人名称-->
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.discovery.DeviceScanActivity" />

        <activity android:name=".ui.DeviceActivity" />

        <activity android:name=".ui.ota.LocalOtaActivity" />

        <activity android:name=".ui.config.DeviceConfigActivity" />

        <activity android:name=".ui.language.LanguageActivity" />

        <activity android:name=".ui.battery.BatteryActivity" />

        <activity android:name=".ui.contacts.ContactActivity" />

        <activity android:name=".ui.contacts.EmergencyContactActivity" />

        <activity android:name=".ui.alarm.AlarmActivity" />

        <meta-data
            android:name="persimwear"
            android:value="a1c4qbha2eujv42rl5amgttqunpl4kng"/>

    </application>

</manifest>